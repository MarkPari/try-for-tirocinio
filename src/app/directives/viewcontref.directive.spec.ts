import { ViewcontrefDirective } from './viewcontref.directive';

describe('ViewcontrefDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewcontrefDirective();
    expect(directive).toBeTruthy();
  });
});
